{
    "name": "current-post-terms-query",
    "version": "1.0.0",
    "private": true,
    "description": "Editor controls for filtering Terms Query blocks by the current post's terms.",
    "author": "Ralf Hortt <mail@ralfhortt.dev>",
    "scripts": {
        "build": "wp-scripts build src/js/index.tsx --output-path=build",
        "start": "wp-scripts start src/js/index.tsx --output-path=build"
    },
    "devDependencies": {
        "@wordpress/scripts": "^32.2.0"
    }
}
